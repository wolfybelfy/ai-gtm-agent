# AI GTM Agent operating rules

Start in this repository root, but treat `trigger-system/` as the only active runtime. Read these files before changing behavior:

1. `trigger-system/CLAUDE.md`
2. `trigger-system/context/field-intelligence.md`
3. `trigger-system/context/play-rules.md`
4. `trigger-system/config/commercial-truth.json`
5. `trigger-system/config/signal-policy.json`

Never edit or run commands inside the sibling `ICP Converstion Intelligence` project. It is the live TAL system and is outside this project's write scope.

The active providers are public evidence sources, ZoomInfo after STRIKE, and Outlook draft creation. No automatic send path exists. Excluded integrations are not to be reintroduced without a new user decision.

Use `docs/superpowers/specs/2026-08-31-open-market-strike-agent-design.md` for the approved design and `docs/superpowers/plans/2026-08-31-open-market-strike-agent.md` for the implementation record.
